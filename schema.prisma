// schema.prisma
model User {
  id                  Int      @id @default(autoincrement())
  email               String   @unique
  publicKey           String
  secretKeyEncrypted  String
  createdAt           DateTime @default(now())
}